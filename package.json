{
  "name": "my-os",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cd ./packages/os-js && npm run build",
    "package:discover": "cd ./packages/os-js && npm run package:discover",
    "db:migrate": "cd ./packages/db-migrate && npm start",
    "start:server": "cd ./packages/os-js && npm run serve",
    "postinstall": "lerna bootstrap && npm run package:discover && npm run build && npm run db:migrate"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "lerna": "^3.6.0"
  }
}
